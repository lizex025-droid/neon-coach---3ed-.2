# دليل التثبيت والتشغيل — NEON COACH

دليل شامل لتهيئة وتشغيل وبناء تطبيق التدريب والتغذية الذكي **NEON COACH**.

---

## 1. المتطلبات الأساسية (Prerequisites)

- **Node.js**: الإصدار 18 أو أحدث (موصى به Node v20 أو v22).
- **مدير الحزم**: `npm` الإصدار 9 أو 10.
- متصفح حديث يدعم PWA و ES Modules (Chrome, Edge, Safari, Firefox).

---

## 2. خطوات التثبيت السريعة (Quick Start)

1. افتح الطرفية (Terminal) داخل مجلد المشروع:
   ```bash
   cd "c:\Users\User\Desktop\neon coach"
   ```

2. تثبيت الحزم:
   ```bash
   npm install
   ```

3. تشغيل خادم التطوير المحلي:
   ```bash
   npm run dev
   ```
   سيظهر لك رابط تشغيل محلي فوري (مثل `http://localhost:3000`).

---

## 3. تشغيل الاختبارات البرمجية (Testing)

تم بناء حزمة اختبارات وحدة كاملة للتحقق من المعادلات الرياضية وحسابات الماكروز ومولد التمارين:
```bash
npm test
```
ينفذ 13 اختباراً فحصاً شاملاً لـ:
- معادلات BMR و TDEE و Mifflin-St Jeor.
- التطابق التام للسعرات الحرارية مع الماكروز.
- معادلة القوة Epley e1RM.
- توليد قوالب التدريب لـ 1 إلى 6 أيام وموانع الإصابات.
- تحليل نصوص الوجبات باللغة العربية وفلترة مسببات الحساسية.

---

## 4. بناء المشروع للإنتاج (Production Build)

لبناء ملفات الويب المحسنة والمصغرة في مجلد `dist/`:
```bash
npm run build
```

لمعاينة النسخة المبنية محلياً قبل النشر:
```bash
npm run preview
```

---

## 5. إعداد الذكاء الاصطناعي (اختياري)

لا تضع مفاتيح Gemini أو OpenAI في متغيرات تبدأ بـ `VITE_` لأنها تُضمّن في ملفات المتصفح. إعداد المزود الحي يجب أن يتم في خدمة خلفية تحتفظ بالمفتاح على الخادم، ثم يستدعي التطبيق نقطة النهاية المحمية فقط.

---

## 6. تثبيت الـ PWA والعمل دون اتصال (Offline-First)

- التطبيق مزود بـ `manifest.json` و `sw.js` مسجل.
- عند فتح التطبيق على الجوال أو المتصفح، سيظهر خيار "تثبيت التطبيق على الشاشة الرئيسية".
- تحفظ ذاكرة التخزين المؤقت الملفات الثابتة فقط. تسجيل الدخول والمزامنة يحتاجان اتصالاً آمناً بـ Supabase.
